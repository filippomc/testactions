name: 'Push'
on: ['push']

jobs:
  build:
    runs-on: 'ubuntu-latest'
    environment: dev

    steps:
    - uses: 'actions/checkout@v1'

    - name: 'Test'
      run: 'echo ${{ env.VAR_1 }} ${{ vars.VAR_1 }} ${{ env.SECRET_1 }}'

    # - name: 'Build'
    #   run: 'docker build -t myapp .'
    # - name: 'Push'
    #   run: 'docker push myapp'
